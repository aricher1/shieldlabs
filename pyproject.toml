[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "xrct-shielding"
version = "0.1.0"
description = "Geometry-first radiation shielding solver"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
